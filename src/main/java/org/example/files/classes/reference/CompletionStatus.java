package org.example.files.classes.reference;

/**
 * Enum class that holds all the completion status for the tasks
 */
public enum CompletionStatus {
    COMPLETE, INCOMPLETE, PENDING, IN_PROGRESS, NOT_STARTED, ON_HOLD, CANCELLED, ABANDONED, REPETITIVE_WEEKLY, REPETITIVE_MONTHLY
}

